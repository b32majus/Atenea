# G1 execution repository

This repository is a blank execution surface for the Atenea vNext shaping benchmark.

The run harness supplies the common product brief separately. Do not assume any additional behavior beyond that brief and the repository's normal engineering policy.
